DATABASE_URL="postgresql+psycopg://postgres:pass@localhost:5432/afc_inventory"
PORT=5050
FLASK_APP=backend/inventory_api.py
FLASK_ENV=development
# Shared secret for QB agent → backend authentication (Bearer token).
# Set to a strong random value in production; leave empty to disable auth.
QB_AGENT_API_KEY=